# .github/workflows/deploy-staging.yml

# ─────────────────────────────────────────────────────────────────────────

# Lean deploy — Docker already installed by setup-staging.yml.

# Trigger: manual only (workflow_dispatch).

# reason is optional for staging — required on production.

#

# No Docker required on yourname-runner — uses native ssh and scp only.

# ─────────────────────────────────────────────────────────────────────────


name: "Deploy — Staging"


on:

  workflow_dispatch:

    inputs:

      reason:

        description: 'Reason for this deployment (e.g. QA approved, ticket PROJ-142)'

        required: false    # Optional for staging — required: true on production


jobs:

  deploy:

    name: "Deploy to Staging"

    runs-on: self-hosted


    steps:

      # Step 1: Check out the code so scp can access the files

      - name: Checkout

        uses: actions/checkout@v4


      # Step 2: Write the reason to the run log — permanent audit trail entry

      # github.actor = GitHub username of whoever clicked Run workflow

      - name: Log deployment reason

        run: echo "STAGING DEPLOY — ${{ github.event.inputs.reason || 'no reason provided' }} — ${{ github.actor }}"


      # Step 3: Copy app files
      - name: Copy app files to Staging VM

        run: |

          echo "${{ secrets.STAGING_SSH_KEY }}" > /tmp/staging_key

          chmod 600 /tmp/staging_key
          # Simplified: No backslashes, no .dockerignore
          scp -i /tmp/staging_key -o StrictHostKeyChecking=no -r app/ Dockerfile docker-compose.yml ${{ secrets.STAGING_USER }}@${{ secrets.STAGING_HOST }}:/opt/app/
          rm /tmp/staging_key


      # Step 4: Build and start the container



      - name: Deploy Flask container on Staging

        run: |

          echo "${{ secrets.STAGING_SSH_KEY }}" > /tmp/staging_key
          
          chmod 600 /tmp/staging_key
          # Run everything in one simple SSH command string
          ssh -i /tmp/staging_key -o StrictHostKeyChecking=no ${{ secrets.STAGING_USER }}@${{ secrets.STAGING_HOST }} "cd /opt/app && sudo HOST_PORT=5001 ENVIRONMENT=staging docker compose down --remove-orphans && sudo HOST_PORT=5001 ENVIRONMENT=staging docker compose up -d --build && sudo docker compose ps"
          rm /tmp/staging_key

      # Step 5: Smoke test
      - name: Smoke test
        run: |
          sleep 5
          curl -f "http://${{ secrets.STAGING_HOST }}:5001/health" | python3 -m json.tool


      # Step 5: Smoke test — confirms the app is up and responding

      # curl -f returns a non-zero exit code on 4xx/5xx — fails the whole run

      - name: Smoke test

        run: |

          sleep 4

          curl -f "http://${{ secrets.STAGING_HOST }}:5001/health" | python3 -m json.tool

          echo '=== Staging smoke test passed ==='
